import pygame,math,sys,random
  
  class Car:
    def __init__(self,num,image,turbimage="turbo.png"):
      if num==1:
        self.x=
        self.y=
      else:
        self.x=
        self.y=
      self.pos=(self.x,self.y)
      self.onground=true
      self.image=pygame.load.image(image)
      self.turbo=100
      self.isturbo==0
      self.turbimage==pygame.load.image(turbimage)
  
    def Onground(self):
      if self.y>= :
        self.y= 
        self.onground=true
      elif self.y<= :
        self.y= 
        self.onground=true
    def Draw(self):
        if isturbo:

    
    
    
  
      
